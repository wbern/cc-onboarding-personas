---
import WizardLayout from '../../layouts/WizardLayout.astro';
import TerminalMockup from '../../components/TerminalMockup.astro';
import CopyButton from '../../components/CopyButton.tsx';

const installCommand = 'curl -fsSL https://claude.ai/install.sh | bash';
const base = import.meta.env.BASE_URL;
---
<WizardLayout
  title="Install"
  currentStep={3}
  totalSteps={6}
  prevHref={`${base}craftsman/platform`}
  nextHref={`${base}craftsman/login`}
>
  <h1 class="text-3xl font-bold text-white mb-2">Install Claude Code</h1>
  <p class="text-slate-400 mb-6">
    Open a terminal and run this.
  </p>

  <div class="relative mb-6">
    <TerminalMockup command={installCommand} />
    <div class="absolute top-2.5 right-3">
      <CopyButton text={installCommand} client:load />
    </div>
  </div>

  <div class="space-y-3">
    <TerminalMockup
      command={installCommand}
      output="Installing Claude Code...
Downloading latest version...
Installation complete! Run 'claude' to get started."
    />

    <p class="text-sm text-slate-500">
      Official installer from Anthropic. Takes a few seconds.
    </p>
  </div>
</WizardLayout>
